{
  "name": "bridge-server",
  "version": "1.0.0",
  "private": true,
  "description": "Render-ready relay server for mobile client to local agent task routing.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
